package org.xebia.mowitnow.domain.service;

import org.xebia.mowitnow.domain.model.instructions.MoveInstruction;

public interface MoveInstructionService {
    void move(MoveInstruction moveInstruction);
}
